# Python Program to Calculate the Area of a Rectangle (Using loops to ensure input is positive)
# The program also uses if and while loops to allow the user to repeat the operation

print ("Programme to Calculate the area of a rectangle (or square)") 

def calculate_area():
    print(" ") # Prints a blank line for readability and neatness

    length = input('Please enter the length of the rectangle:')
    
    # The WHILE statement is a LOOPING statement. It executes as long as the length inputed by the user is not a valid number
    while not length.isdigit():
        print("Error: Please enter a valid number")
        length = input("Please enter the length of the rectangle:")
    
    # This converts the length to an integer to check it is positive
    length = int(length) 

    while length <= 0 :
        print ("Length must be positive. Try again")
        length = input("Please enter the length of the rectangle:")
       
        while not length.isdigit():
            print("Error: Please enter a valid number")
            length = input('Please enter the length of the rectangle:') #checks again that length is a number
    
    #This converts the length to a number after validation
    length = int(length) 

    # The IF statement below is a CONDITIONAL statement. It only executes once the length inputted by the user is positive. 
    if length > 0:
        # When a positive length is entered, the programme proceeds to request the width  
        width = input("Please enter the width of the rectangle: ")

        while not width.isdigit():
            print("Error: Please enter a valid number")
            width = input('Please enter the width of the rectangle:')

        width = int(width) #This converts the width to integer to check it is positive
    
       # Now the width is a number, so Python checks if it is positive
        while width <= 0 :  
            print ("\nThe width must be positive.Try again")
            width = input('Please enter the width of the rectangle:')

            # This checks again that width is a number
            while not width.isdigit(): 
                print("Error: Please enter a valid number")
                width = input("Please enter the width of the rectangle:")

                width = int(width) # This converts the width to a integer after validation

        if width > 0:

            area = length * width 
            # The programme proceeds with calculation when both length and width are positive
            print("\n The Area is", area)
            print(" ") # A blank line is left for readability and neatness
            return area
                

calculate_area() # This core line calls the calculate_area function.

# This code section gives the user the option to repeat the calculation
ans = int(input("Would you like to do this again. \n  1. Yes \n  2. No  \n"))
while ans == 1:
    calculate_area()
    ans = int(input("Would you like to do this again. \n  1. Yes \n  2. No \n ")) 

         
 # This code section ends the program when the user confirms they do not want to repeat the operation
else:
    closing = input("Thanks for using the program. Bye! Press Enter to exit.")
    quit()
 
